/Question partial, should use the given instrument to generate a question :
  => guess instrument from image
  => guess instrument from sound
  => other way around ??

question.w-full.rounded.overflow-hidden.shadow-lg.m-2.flex.flex-col.items-center
  - if instrument
    .px-6.py-4.text-center
      h2.font-bold.text-xl
        = instrument.name
      p
        = '[TEMP] Question content (images, sounds, etc) + add drag & drop ?'

  - else
    .px-6.py-4.text-center
      h2.font-bold.text-xl
        = '[TEMP] No instrument found ⌒(๑•́ω•̀๑)⌒'


  - if instrument
    .buttons.border.w-full.flex.items-center.justify-around
      = link_to "Previous", previous_instrument_exercise_path(@exercise, instrument_id: instrument.id), data: { turbo_frame: "question" }, class: 'bg-blue-600 hover:bg-blue-800 text-white font-bold py-2 px-4 mt-6 rounded focus:outline-none focus:shadow-outline'
      = link_to "Next", next_instrument_exercise_path(@exercise, instrument_id: instrument.id), data: { turbo_frame: "question" }, class: 'bg-blue-600 hover:bg-blue-800 text-white font-bold py-2 px-4 mt-6 rounded focus:outline-none focus:shadow-outline'